import styled from 'styled-components';

export const Form = styled.div`

  & > form {
  display: flex;
  flex-direction: column;
  padding: 2em;

    & > div {
      margin-bottom: 0.7em;
    }
    
  }

  
`;
